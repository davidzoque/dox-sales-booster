<?php
/**
 * Versión de esta copia de dox-core.
 *
 * Es el único sitio donde se escribe. El plugin la lee de aquí al apuntarse en
 * el cargador, así que copiar la carpeta a un plugin ya le cambia la versión:
 * antes iba escrita a mano en el archivo principal de cada plugin, y bastaba
 * olvidarse de uno para que una copia nueva compitiera con número viejo.
 *
 * Súbela con CUALQUIER cambio dentro de dox-core, por pequeño que sea. Con dos
 * copias distintas y el mismo número, gana una cualquiera de las dos.
 */

if ( ! defined( 'ABSPATH' ) ) exit;

return '1.0.1';
